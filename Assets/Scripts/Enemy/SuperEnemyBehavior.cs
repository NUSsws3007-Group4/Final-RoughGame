using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SuperEnemyBehavior : EnemyBehavior
{
    // Start is called before the first frame update
    void Start()
    {
        mLifeLeft = 120;
    }

    // Update is called once per frame
    void Update()
    {

    }
}
